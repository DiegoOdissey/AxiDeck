# AxiDeck Backend — Python dependencies
# Install with: pip install -r requirements.txt

pyserial>=3.5
pystray>=0.19.5
Pillow>=10.0.0

# For the GUI (future — install when ready)
# customtkinter>=5.2.0
